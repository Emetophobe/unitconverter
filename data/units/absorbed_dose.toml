# Note that Gray and Sievert share the same dimension
dimension = {"length" = 2, "time" = -2}


# gray is the SI unit of absorbed dose
["gray"]
symbols = ["Gy"]
aliases = ["grays"]
prefix = "metric"
factor = 1

# Rad is an older CGS unit
["rad unit"]
symbols = ["Rad"]  # rad is used by radian
factor = 0.01
