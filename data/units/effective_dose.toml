# Note that Gray and Sievert share the same dimension
dimension = {"length" = 2, "time" = -2}


# The sievert is the SI unit of effective dose
["sievert"]
symbols = ["Sv"]
aliases = ["sieverts"]
prefix = "metric"
factor = 1

# The roetgen is an older CGS unit
["roentgen"]
symbols = ["rem"]
aliases = ["roentgens"]
prefix = "metric"
factor = 0.01
