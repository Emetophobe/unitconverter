dimension = {"luminous intensity" = 1}


# The candela is the SI unit of luminous intensity
["candela"]
symbols = ["cd"]
aliases = ["candelas"]
prefix = "metric"
factor = 1

# 1 candlepower is equivalent to 1 candela
["candlepower"]
symbols = ["cp"]
factor = 1

# 1 historic candlepower = 0.981 candela
["historic candlepower"]
symbols = ["hcp"]
factor = 0.981
